# Ruta opcional para la base SQLite. Por defecto: ./data/panda-gestion.db
DATABASE_PATH=./data/panda-gestion.db

# Configuración opcional de Docker Compose.
# Por seguridad escucha sólo en localhost; Nginx puede actuar como proxy.
PANDA_BIND_ADDRESS=127.0.0.1
PANDA_PORT=3000
